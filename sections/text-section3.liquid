<style>
    .custom-support-{{ section.id }} {
      background-color: {{section.settings.bg_section_color}};
    }
    .custom-support-{{ section.id }} .content h2 {color:{{section.settings.title_color}};}
    .custom-support-{{ section.id }} .content em, 
    .custom-support-{{ section.id }} .content p{color:{{section.settings.text_color}};}
</style>

<div data-section-id="{{ section.id }}" data-section-type="custom-support-section">
  <div class="custom-support custom-support-{{ section.id }}" style="margin-top: {{section.settings.margin_top | remove: 'px' | append: 'px' }};margin-bottom: {{section.settings.margin_bottom | remove: 'px' | append: 'px' }};padding: {{section.settings.section_padding}};">
    <div class="container">
      <div class="shop-features">
        <div class="row">
          {% if section.blocks.size > 0 %} 
          {% for block in section.blocks %}
          <div class="col-md-12">
            <div class="content">
              <h2>
                {% if settings.language_enable %}
                <span class="lang1">{{ block.settings.item_title | split: '|' | first }}</span>
                <span class="lang2">{{ block.settings.item_title | split: '|' | last }}</span>
                {% else %}
                {{ block.settings.item_title | split: '|' | first }}
                {% endif %}
              </h2>

              {% if settings.language_enable %}
              <span class="lang1">{{ block.settings.item_text | split: '|' | first }}</span>
              <span class="lang2">{{ block.settings.item_text | split: '|' | last }}</span>
              {% else %}
              {{ block.settings.item_text | split: '|' | first }}
              {% endif %}
            </div>
          </div> 
          {% endfor %}
          {% else %}
          <p>No Content</p> 
          {%endif%}
          <div style="clear: both;"></div>
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Text Block 3",
  "max_blocks": 1,
  "settings": [ 
    {
      "type": "header",
      "content": "Section Settings"
    },
    {
      "type": "color",
      "id": "bg_section_color",
      "label": "Section background color" ,
      "default": "#ffffff"
    }, 
    {
      "type": "text",
      "id": "margin_top",
      "label": "Section margin top",
      "info": "40px"
    },
    {
      "type": "text",
      "id": "margin_bottom",
      "label": "Section margin bottom",
      "info": "40px"
    },
    {
      "type": "text",
      "id": "section_padding",
      "label": "Section padding",
      "info": "Set padding for section top-right-bottom-left (ex: 0 0 10px 0)"
    },
    {
      "type": "header",
      "content": "Content Settings"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color"
    }
  ],
  "blocks": [
    {
      "type": "item-block",
      "name": "Item",
      "settings": [
        {
          "type": "text",
          "id": "item_title",
          "label": "Item Tile",
          "default": "Title"
        },
        {
          "type": "html",
          "id": "item_text",
          "label": "Item Text",
          "default": "Content"
        }
      ]
    }
  ],
  "presets": [{
    "name": "Text Block 3",
    "category": "Text Section", 
    "blocks": [
      {
        "type": "item-block",
        "settings": {
          "item_title": "Customer Support",
		  "item_text": "<em>YOU WON'T BE ALONE</em><p>We really care about you and your website as much as you do. Purchasing Porto or any other theme from us you get 100% free support.</p>"
        }
      }
    ]
  }]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
